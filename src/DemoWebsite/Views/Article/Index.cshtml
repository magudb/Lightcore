@using System.Threading.Tasks
@using Lightcore.Kernel.Mvc
@model Lightcore.Kernel.Data.Item
@await Html.Lightcore().RenderFieldAsync("Image")
<article>
    <h2>@Model["Title"]</h2>
    @Html.Lightcore().Field("Text")
    <p>
        @await Html.Lightcore().RenderFieldAsync("General Link", new {@class = "pretty", title = "Click me!"})
    </p>
</article>
@await Html.Lightcore().PlaceholderAsync("sidebar")